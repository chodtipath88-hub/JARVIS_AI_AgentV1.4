# JARVIS V1.2 – อัปเดตวันที่ 2025-10-28

เอกสารนี้สรุปงานที่ดำเนินการในวันนี้ และวิธีการใช้งานระบบทั้ง Web และ Desktop บน Windows ให้ใช้งานได้จริงแบบรวดเร็ว

## สรุปสิ่งที่ทำวันนี้

- เพิ่มหน้า “สมัครสมาชิก” แบบแยกไฟล์ (Dedicated Register Page)
  - สร้างไฟล์ใหม่ `register.html` (ภาษาไทย, UTF-8) พร้อมฟอร์มสมัครสมาชิกและเรียก API `POST /register`
  - เพิ่มเส้นทาง `/register-page` ใน `web_server.py` เพื่อเสิร์ฟไฟล์ `register.html`
- ปรับปรุงหน้าหลักรวม (Login + Chat) ให้ใช้งานและดูดีขึ้น
  - ปรับ `jarvis_unified.html` ให้มีธีม Light/Dark สลับได้ พร้อมจดจำค่าใน `localStorage`
  - เพิ่ม Avatar สำหรับผู้ใช้ ("คุณ") และผู้ช่วย ("🤖") ในฟองแชต คล้าย ChatGPT
  - ปรับโทนสีฟองแชต, Sidebar, Header, ช่องพิมพ์ข้อความ ให้สวยงามและอ่านง่ายขึ้น
  - เพิ่มลิงก์ “ไปที่หน้าสมัครสมาชิกแบบเต็ม” เชื่อมไป `/register-page`
- ตรวจสุขภาพระบบและยืนยันความพร้อม
  - รันเซิร์ฟเวอร์และตรวจ `/health` ได้ค่าปกติ: DB เชื่อมต่อ, Ollama พร้อมใช้งาน, Models: `phi3:mini`, `qwen3-coder:480b-cloud`

หมายเหตุ: โค้ดแกนหลัก (เช่น ตัว Agent และฝั่งเว็บ) มีการป้องกัน error และ thread-safety สำหรับ SQLite อยู่แล้วจากรอบก่อนหน้า ทำให้ระบบเสถียรขึ้นเมื่อมีการใช้งานหลายเธรด

## วิธีใช้งาน (Windows)

### 1) เริ่ม Web Interface (แนะนำ)

- ทางลัด: ดับเบิลคลิก `start_jarvis_web.bat` (จะตั้งค่า OLLAMA_HOST/Model ให้อัตโนมัติและเปิดที่ http://localhost:5000)
- หรือเปิด PowerShell แล้วรัน:

```powershell
python .\web_server.py
```

จากนั้น:
- เปิดเบราว์เซอร์ไปที่ http://localhost:5000
- เข้าสู่ระบบด้วยบัญชีตัวอย่าง: `admin` / `admin123`
- หรือสมัครสมาชิกใหม่ได้ 2 วิธี:
  - คลิก “ยังไม่มีบัญชี? สมัครสมาชิก” เพื่อเปิดฟอร์มสมัครแบบอินไลน์
  - คลิก “ไปที่หน้าสมัครสมาชิกแบบเต็ม” เพื่อไปที่ `/register-page`
- เข้าสู่ห้องแชต พิมพ์ข้อความ แล้วกด “ส่ง”
- เปลี่ยนธีมได้ด้วยปุ่ม “สลับธีม” บริเวณมุมขวาบน
- รายการบทสนทนาจะอยู่ด้านซ้าย คลิกเพื่อย้อนดูประวัติได้
- ปุ่ม “ออกจากระบบ” จะตัด session และล้างคุกกี้

ทิปสำคัญ:
- ห้ามเปิดไฟล์ HTML ตรงๆ ด้วย `file://` เพราะจะเรียก API ไม่ได้ ต้องใช้งานผ่าน URL ของเซิร์ฟเวอร์เท่านั้น (เช่น http://localhost:5000)

### 2) ตรวจสุขภาพระบบและคอนฟิก

- ตรวจสุขภาพ: http://localhost:5000/health  (ดูสถานะ DB, Ollama, models)
- ดูคอนฟิกที่ใช้อยู่: http://localhost:5000/config  (OLLAMA_HOST, DEFAULT_MODEL, TIMEOUT, RETRIES)

### 3) Desktop App (Browser-based, Streamlit)

- ทางลัด: ดับเบิลคลิก `start_desktop_streamlit.bat` (จะเปิดเบราว์เซอร์ที่ http://localhost:8501)
- หรือเปิด PowerShell แล้วรัน:

```powershell
streamlit run .\desktop_app_streamlit.py
```

การใช้งาน:
- พิมพ์ถาม–ตอบได้เหมือนหน้าเว็บ และเลือกโมเดล/ดูสถิติการใช้งานได้ภายในแอป

### 4) Desktop App (Native, PyQt5)

- ทางลัด: ดับเบิลคลิก `start_desktop_pyqt5.bat`
  - สคริปต์จะตรวจว่ามี PyQt5 แล้วหรือยัง หากไม่มีจะหยุดค้างไว้ให้ติดตั้งและลองใหม่
- หรือเปิด PowerShell แล้วรัน:

```powershell
python .\desktop_app_pyqt5.py
```

การใช้งาน:
- หน้าต่างเดสก์ท็อปพร้อมฟองแชตสไตล์ ChatGPT มีแถบเมนูสำหรับบันทึกบทสนทนาเป็น TXT/PDF และดูประวัติฝั่งซ้าย

### 5) Mini Server (เบา ๆ ไม่ต้องล็อกอิน)

- ทางลัด: ดับเบิลคลิก `start_mini_server.bat`
- หรือเปิด PowerShell แล้วรัน:

```powershell
python .\mini_server.py
```

## การติดตั้งแพ็กเกจ (ครั้งแรกเท่านั้น)

หากยังไม่ได้ติดตั้งไลบรารีที่จำเป็น ให้รันใน PowerShell (ครั้งเดียวพอ):

```powershell
pip install -r .\requirements.txt
```

ถ้าใช้ Desktop PyQt5 แล้วเจอว่าไม่มีไลบรารี ให้ติดตั้งเพิ่ม:

```powershell
pip install PyQt5
```

## ทดสอบความพร้อมแบบรวดเร็ว (ที่ทำให้แล้ววันนี้)

- รันเว็บเซิร์ฟเวอร์ แล้วเปิด http://localhost:5000 — ทำงานปกติ
- เรียก http://localhost:5000/health — สถานะ OK, DB เชื่อมต่อ, Ollama พร้อม, Models: `phi3:mini`, `qwen3-coder:480b-cloud`
- สมัครสมาชิกผ่าน `/register-page` แล้วกลับไปหน้าแรกเพื่อเข้าสู่ระบบ — สำเร็จ
- ทดลองสลับธีม Light/Dark และส่งข้อความ — ใช้งานได้

## ไฟล์ที่เพิ่ม/แก้ไขวันนี้

- เพิ่ม: `register.html` — หน้าสมัครสมาชิกแบบเต็ม (Thai, UTF-8)
- แก้ไข: `jarvis_unified.html` — ปรับธีม Light/Dark, เพิ่ม Avatar และลิงก์ไปหน้า Register

(ไฟล์อื่น ๆ ที่พร้อมใช้งานอยู่แล้วและใช้ร่วมด้วย: `web_server.py`, `start_jarvis_web.bat`, `desktop_app_streamlit.py`, `start_desktop_streamlit.bat`, `desktop_app_pyqt5.py`, `start_desktop_pyqt5.bat`)

## ข้อควรทราบและแก้ปัญหาเบื้องต้น

- หากล็อกอินไม่ได้ ให้ตรวจว่าเปิดผ่าน http://localhost:5000 ไม่ใช่ `file://` และดู `/health` ว่า Ollama พร้อมหรือไม่
- หาก Desktop PyQt5 ไม่ขึ้น ให้ติดตั้ง `PyQt5` ตามคำสั่งด้านบน แล้วลองรันใหม่
- หากโมเดลของ Ollama ไม่เจอ ให้เช็คตัวแปรแวดล้อมที่ตั้งไว้ในไฟล์ `.bat` หรือรันเซิร์ฟเวอร์ Ollama ให้พร้อมก่อน

---

ต้องการให้เพิ่มธีม/ฟีเจอร์เพิ่มเติม (เช่น ปุ่มคัดลอกข้อความ, แสดงโค้ดบล็อกสวยงาม, ดาวน์โหลดประวัติทั้งหมด) แจ้งได้เลย พร้อมทำต่อให้ครับ 🙂
